[project]
name = "personal-finance-fastapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "~=3.13.0"
dependencies = [
    "fastapi[standard]>=0.139.2",
    "pre-commit>=4.6.1",
    "requests>=2.34.2",
]
